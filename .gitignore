node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Runtime data (certificates, curricula) — tracked via .gitkeep, data populated at runtime
data/certificates.json
data/curricula.json