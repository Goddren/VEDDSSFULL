node_modules
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Runtime data
data/certificates.json
data/curricula.json

# Env / secrets (never commit real values; .env.example is the safe template)
.env
.env.*
!.env.example
