node_modules
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Runtime data
data/certificates.json
data/curricula.json
# Build output — keep server bundle in git, ignore client build (Render builds it fresh)
dist/public/
